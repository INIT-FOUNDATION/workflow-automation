import { ExactParticipantsCountPipe } from './exact-participants-count.pipe';

describe('ExactParticipantsCountPipe', () => {
  it('create an instance', () => {
    const pipe = new ExactParticipantsCountPipe();
    expect(pipe).toBeTruthy();
  });
});
