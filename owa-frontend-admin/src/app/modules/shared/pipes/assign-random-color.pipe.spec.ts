import { AssignRandomColorPipe } from './assign-random-color.pipe';

describe('AssignRandomColorPipe', () => {
  it('create an instance', () => {
    const pipe = new AssignRandomColorPipe();
    expect(pipe).toBeTruthy();
  });
});
