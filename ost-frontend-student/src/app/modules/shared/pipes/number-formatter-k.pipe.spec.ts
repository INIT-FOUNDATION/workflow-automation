import { NumberFormatterKPipe } from './number-formatter-k.pipe';

describe('NumberFormatterKPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberFormatterKPipe();
    expect(pipe).toBeTruthy();
  });
});
