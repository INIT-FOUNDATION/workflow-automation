import { VcActionIconsPipe } from './vc-action-icons.pipe';

describe('VcActionIconsPipe', () => {
  it('create an instance', () => {
    const pipe = new VcActionIconsPipe();
    expect(pipe).toBeTruthy();
  });
});
