import { BootstrapGridColPipe } from './bootstrap-grid-col.pipe';

describe('BootstrapGridColPipe', () => {
  it('create an instance', () => {
    const pipe = new BootstrapGridColPipe();
    expect(pipe).toBeTruthy();
  });
});
